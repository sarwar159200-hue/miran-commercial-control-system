import Link from "next/link";import {createInvoice} from "../../_actions/commercial";import {createClient} from "@/lib/supabase/server";export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams,s=await createClient();if(!s)return null;const [pr,vr,cr,mr,pf,ur,rr]=await Promise.all([s.from("purchase_orders").select("id,po_number,vendor_id,currency_id").eq("is_deleted",false).order("po_number"),s.from("vendors").select("id,vendor_name").eq("is_active",true).order("vendor_name"),s.from("currencies").select("id,code").eq("is_active",true).order("code"),s.from("payment_milestones").select("id,purchase_order_id,milestone_name").order("sequence_no"),s.from("profiles").select("id,full_name,preferred_name,is_active").eq("is_active",true),s.from("user_roles").select("user_id,role_id"),s.from("roles").select("id,role_code")]);const allowed=new Set((rr.data??[]).filter((r:any)=>["discipline_engineer","project_manager","commercial","admin","super_admin"].includes(String(r.role_code))).map((r:any)=>String(r.id)));const reviewerIds=new Set((ur.data??[]).filter((x:any)=>allowed.has(String(x.role_id))).map((x:any)=>String(x.user_id)));const reviewers=(pf.data??[]).filter((p:any)=>reviewerIds.has(String(p.id)));return <div className="mx-auto max-w-5xl"><div className="flex justify-between"><div><div className="text-xs font-extrabold uppercase tracking-[.2em] text-blue-700">Invoice Assurance</div><h1 className="mt-2 text-3xl font-bold">New Invoice</h1><p className="mt-2 text-sm text-slate-500">Register invoice and optionally assign the technical reviewer immediately.</p></div><Link href="/invoices" className="font-bold text-blue-700">Back</Link></div>{q.error?<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{q.error}</div>:null}<form action={createInvoice} className="mccs-card mt-7 rounded-2xl p-6"><div className="grid gap-5 md:grid-cols-2"><F l="PO *"><select name="purchase_order_id" required className="input"><option value="">Select PO</option>{(pr.data??[]).map((v:any)=><option key={v.id} value={v.id}>{v.po_number}</option>)}</select></F><F l="Vendor *"><select name="vendor_id" required className="input"><option value="">Select vendor</option>{(vr.data??[]).map((v:any)=><option key={v.id} value={v.id}>{v.vendor_name}</option>)}</select></F><F l="Milestone"><select name="milestone_id" className="input"><option value="">Not linked</option>{(mr.data??[]).map((v:any)=><option key={v.id} value={v.id}>{v.milestone_name}</option>)}</select></F><F l="Currency *"><select name="currency_id" required className="input"><option value="">Select currency</option>{(cr.data??[]).map((v:any)=><option key={v.id} value={v.id}>{v.code}</option>)}</select></F><F l="Invoice Number *"><input name="invoice_number" required className="input"/></F><F l="Invoice Date *"><input name="invoice_date" type="date" required className="input"/></F><F l="Received Date"><input name="received_date" type="date" className="input"/></F><F l="Due Date"><input name="due_date" type="date" className="input"/></F><F l="Invoice Amount *"><input name="invoice_amount" type="number" step="0.01" required className="input"/></F><F l="Certified Amount"><input name="certified_amount" type="number" step="0.01" defaultValue="0" className="input"/></F><F l="Initial Reviewer"><select name="assigned_reviewer_id" className="input"><option value="">Assign later</option>{reviewers.map((p:any)=><option key={p.id} value={p.id}>{p.preferred_name||p.full_name}</option>)}</select></F><F l="Historical Source"><input name="historical_source" className="input" placeholder="Legacy register / email / old system"/></F></div><label className="mt-5 flex items-center gap-3 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm font-semibold text-amber-900 dark:border-amber-900/50 dark:bg-amber-950/30 dark:text-amber-200"><input type="checkbox" name="is_historical"/> Historical / previously processed invoice</label><F l="Verification Notes"><textarea name="verification_notes" rows={4} className="input mt-5"/></F><div className="mt-7 flex justify-end"><button className="rounded-xl bg-[#07111f] px-5 py-3 text-sm font-bold text-white dark:bg-blue-600">Create Invoice</button></div></form></div>}
function F({l,children}:{l:string;children:React.ReactNode}){return <label><span className="mb-2 block text-sm font-bold text-slate-700 dark:text-slate-200">{l}</span>{children}</label>}
