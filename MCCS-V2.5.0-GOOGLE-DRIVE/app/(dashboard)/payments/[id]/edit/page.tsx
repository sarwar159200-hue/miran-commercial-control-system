import Link from "next/link";import {updatePayment} from "../../../_actions/commercial";import {createClient} from "@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const {id}=await params,q=await searchParams,s=await createClient();if(!s)return null;const [rr,pr,ir,cr]=await Promise.all([s.from("payments").select("*").eq("id",id).maybeSingle(),s.from("purchase_orders").select("id,po_number").eq("is_deleted",false),s.from("invoices").select("id,invoice_number"),s.from("currencies").select("id,code").eq("is_active",true)]);const x=rr.data;if(!x)return <div>Payment not found.</div>;return <div className="mx-auto max-w-4xl"><div className="flex justify-between"><h1 className="text-3xl font-bold">Edit Payment</h1><Link href="/payments" className="font-bold text-blue-700">Back</Link></div>{q.error?<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-red-700">{q.error}</div>:null}<form action={updatePayment} className="mccs-card mt-7 rounded-2xl p-6"><input type="hidden" name="payment_id" value={id}/><div className="grid gap-5 md:grid-cols-2"><F l="PO *"><select name="purchase_order_id" defaultValue={x.purchase_order_id||""} required className="input">{(pr.data??[]).map((v:any)=><option key={v.id} value={v.id}>{v.po_number}</option>)}</select></F><F l="Invoice"><select name="invoice_id" defaultValue={x.invoice_id||""} className="input"><option value="">Not linked</option>{(ir.data??[]).map((v:any)=><option key={v.id} value={v.id}>{v.invoice_number}</option>)}</select></F><F l="Currency *"><select name="currency_id" defaultValue={x.currency_id||""} required className="input">{(cr.data??[]).map((v:any)=><option key={v.id} value={v.id}>{v.code}</option>)}</select></F><F l="Payment Date *"><input name="payment_date" type="date" defaultValue={x.payment_date||""} required className="input"/></F><F l="Paid Amount *"><input name="paid_amount" type="number" step="0.01" defaultValue={x.paid_amount||0} required className="input"/></F><F l="Payment Reference"><input name="payment_reference" defaultValue={x.payment_reference||""} className="input"/></F><F l="Bank Reference"><input name="bank_reference" defaultValue={x.bank_reference||""} className="input"/></F><F l="Historical Source"><input name="historical_source" defaultValue={x.historical_source||""} className="input"/></F></div><label className="mt-5 flex items-center gap-3"><input type="checkbox" name="is_historical" defaultChecked={!!x.is_historical}/> Historical record</label><F l="Notes"><textarea name="notes" defaultValue={x.notes||""} rows={4} className="input mt-5"/></F><div className="mt-7 flex justify-end"><button className="rounded-xl bg-[#07111f] px-5 py-3 text-sm font-bold text-white dark:bg-blue-600">Save Changes</button></div></form></div>}
function F({l,children}:{l:string;children:React.ReactNode}){return <label><span className="mb-2 block text-sm font-bold text-slate-700 dark:text-slate-200">{l}</span>{children}</label>}