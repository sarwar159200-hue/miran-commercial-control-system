import Link from "next/link";import {createDocumentMetadata} from "../../_actions/commercial";import {createClient} from "@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams,s=await createClient();if(!s)return null;const [pr,vr,mr]=await Promise.all([s.from("purchase_orders").select("id,po_number").eq("is_deleted",false),s.from("vendors").select("id,vendor_name").eq("is_active",true),s.from("payment_milestones").select("id,milestone_name")]);return <div className="mx-auto max-w-4xl"><div className="flex justify-between"><div><h1 className="text-3xl font-bold">Add Document Record</h1><p className="mt-2 text-sm text-slate-500">Register historical or current metadata. OneDrive file attachment follows after Graph configuration.</p></div><Link href="/documents" className="font-bold text-blue-700">Back</Link></div>{q.error?<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-red-700">{q.error}</div>:null}<form action={createDocumentMetadata} className="mccs-card mt-7 rounded-2xl p-6"><div className="grid gap-5 md:grid-cols-2"><F l="Document Type *"><input name="document_type" required className="input" placeholder="PO / Contract / Invoice / Approval"/></F><F l="File Name / Reference *"><input name="file_name" required className="input"/></F><F l="Revision"><input name="revision" className="input"/></F><F l="OneDrive Path (optional)"><input name="onedrive_path" className="input" placeholder="MCCS/POs/..."/></F><F l="PO"><select name="purchase_order_id" className="input"><option value="">Not linked</option>{(pr.data??[]).map((x:any)=><option key={x.id} value={x.id}>{x.po_number}</option>)}</select></F><F l="Vendor"><select name="vendor_id" className="input"><option value="">Not linked</option>{(vr.data??[]).map((x:any)=><option key={x.id} value={x.id}>{x.vendor_name}</option>)}</select></F><F l="Milestone"><select name="milestone_id" className="input"><option value="">Not linked</option>{(mr.data??[]).map((x:any)=><option key={x.id} value={x.id}>{x.milestone_name}</option>)}</select></F></div><label className="mt-5 flex items-center gap-3"><input type="checkbox" name="is_historical"/> Historical document</label><div className="mt-7 flex justify-end"><button className="rounded-xl bg-[#07111f] px-5 py-3 text-sm font-bold text-white dark:bg-blue-600">Save Document Record</button></div></form></div>}
function F({l,children}:{l:string;children:React.ReactNode}){return <label><span className="mb-2 block text-sm font-bold text-slate-700 dark:text-slate-200">{l}</span>{children}</label>}