"use client";
import { useState } from "react";
export function UserForm({roles}:{roles:{role_code:string;role_name:string}[]}) {
  const [message,setMessage]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false);
  async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setError("");setMessage("");setLoading(true);const fd=new FormData(e.currentTarget);const payload=Object.fromEntries(fd.entries());const res=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await res.json();setLoading(false);if(!res.ok){setError(data.error||"Unable to create user");return;}setMessage("User account created successfully.");e.currentTarget.reset();}
  return <form onSubmit={submit} className="mccs-card rounded-2xl p-6"><div className="grid gap-5 md:grid-cols-2"><F l="Full Name *"><input name="full_name" required className="input"/></F><F l="Preferred Name *"><input name="preferred_name" required className="input"/></F><F l="Email *"><input name="email" type="email" required className="input"/></F><F l="Temporary Password *"><input name="password" type="password" minLength={10} required className="input"/></F><F l="Gender"><select name="gender" className="input"><option value="">Not specified</option><option value="male">Male</option><option value="female">Female</option></select></F><F l="Honorific"><input name="honorific" className="input" placeholder="Kak / Mrs. / Mr."/></F><F l="Job Title"><input name="job_title" className="input"/></F><F l="Department"><input name="department" className="input"/></F><F l="Role"><select name="role_code" required className="input">{roles.map(r=><option key={r.role_code} value={r.role_code}>{r.role_name}</option>)}</select></F></div>{error?<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>:null}{message?<div className="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 p-4 text-sm text-emerald-700">{message}</div>:null}<div className="mt-7 flex justify-end"><button disabled={loading} className="rounded-xl bg-[#07111f] px-5 py-3 text-sm font-bold text-white disabled:opacity-60">{loading?"Creating...":"Create Team Account"}</button></div></form>
}
function F({l,children}:{l:string;children:React.ReactNode}){return <label><span className="mb-2 block text-sm font-bold text-slate-700">{l}</span>{children}</label>}
