import { createClient } from "@/lib/supabase/server"; import { UserForm } from "./user-form";
export const dynamic="force-dynamic";
export default async function Page(){const supabase=await createClient();if(!supabase)return null;const r=await supabase.from("roles").select("role_code,role_name").eq("is_active",true).order("role_name");const roles=r.data??[];return <div className="mx-auto max-w-5xl"><div className="text-xs font-extrabold uppercase tracking-[0.2em] text-blue-700">Super Admin</div><h1 className="mt-2 text-3xl font-bold">Create Team Account</h1><p className="mt-2 mb-7 text-sm text-slate-500">Create users and assign their initial authorization role.</p><UserForm roles={roles}/><div className="mt-5 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-800">Requires server-only Vercel variable <b>SUPABASE_SERVICE_ROLE_KEY</b>. Never expose it as NEXT_PUBLIC.</div></div>}