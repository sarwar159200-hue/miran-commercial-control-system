import { createClient } from "@/lib/supabase/server";
export const dynamic="force-dynamic";
export default async function Page(){const supabase=await createClient();if(!supabase)return null;const r=await supabase.from("payment_milestones").select("id,milestone_name,percentage,fixed_amount,planned_due_date,payment_due_date,status,purchase_orders(po_number)").order("planned_due_date");const rows=r.data??[];return <div className="mx-auto max-w-[1500px]"><div className="text-xs font-extrabold uppercase tracking-[0.2em] text-blue-700">MCCS</div><h1 className="mt-2 text-3xl font-bold">Payment Milestones</h1><p className="mt-2 text-sm text-slate-500">Contractual payment obligations and due dates.</p><div className="mccs-card mt-7 overflow-hidden rounded-2xl"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">PO</th><th className="px-5 py-3">Milestone</th><th className="px-5 py-3">%</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Due</th><th className="px-5 py-3">Payment Due</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y">{rows.map((m:any)=><tr key={m.id}><td className="px-5 py-4 font-bold">{m.purchase_orders?.po_number||"—"}</td><td className="px-5 py-4">{m.milestone_name}</td><td className="px-5 py-4">{m.percentage??"—"}</td><td className="px-5 py-4">{m.fixed_amount?Number(m.fixed_amount).toLocaleString():"—"}</td><td className="px-5 py-4">{m.planned_due_date||"—"}</td><td className="px-5 py-4">{m.payment_due_date||"—"}</td><td className="px-5 py-4 capitalize">{m.status}</td></tr>)}{!rows.length?<tr><td colSpan={7} className="px-5 py-12 text-center text-slate-500">No payment milestones yet.</td></tr>:null}</tbody></table></div></div>}