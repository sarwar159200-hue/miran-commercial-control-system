# MCCS V2.1.1 Build Fix

This package fixes the Vercel TypeScript build failure on the Invoices page:

`Type '{}' is not assignable to type 'ReactNode'`

The invoice/payment/register lookup maps now use explicit string/object typings rather than inferred `{}` values. Similar map typing was hardened proactively across Purchase Orders, Vendors, Dashboard, Payment Milestones, and the Super Admin Control Center.

The corrected `007_near_go_live_core.sql` is also included as the active 007 migration.

Vercel Root Directory: `MCCS-V2.1.1-BUILD-FIXED`
Output Directory: leave blank.
