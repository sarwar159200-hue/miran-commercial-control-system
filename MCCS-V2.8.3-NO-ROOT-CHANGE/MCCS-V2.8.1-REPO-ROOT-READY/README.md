# MCCS V2.8.3 — No Root Directory Change

This package is intentionally stored inside the folder:

`MCCS-V2.8.1-REPO-ROOT-READY`

Keep the existing Vercel Root Directory exactly as:

`MCCS-V2.8.1-REPO-ROOT-READY`

Do not change it to V2.8.2 or V2.8.3. The application code is V2.8.3, but the folder name stays V2.8.1 so the existing Vercel project setting continues to point to the correct `package.json`.

Vercel should detect Next.js 15.5.21.
