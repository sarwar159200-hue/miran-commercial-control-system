import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
import SuperAdminDelete from "@/components/super-admin-delete";
import MilestoneVendorFilter from "@/components/milestone-vendor-filter";
import { bulkUpdateMilestones, deletePaymentMilestone } from "../_actions/commercial";

export const dynamic = "force-dynamic";

const statuses = [["planned","Planned"],["awaiting_evidence","Awaiting Evidence"],["eligible","Eligible"],["awaiting_invoice","Awaiting Invoice"],["under_verification","Under Verification"],["certified","Certified"],["submitted_to_ap","Submitted to AP"],["paid","Paid"],["on_hold","On Hold"],["cancelled","Cancelled"]];

type Q={deleted?:string;error?:string;updated?:string;bulk_updated?:string;vendor?:string;package?:string;sort?:string;dir?:string};

export default async function Page({searchParams}:{searchParams:Promise<Q>}){
  const q=await searchParams; const supabase=await createClient(); if(!supabase)return null;
  const {data:{user}}=await supabase.auth.getUser();
  const {data:me}=user?await supabase.from("profiles").select("is_super_admin").eq("id",user.id).maybeSingle():{data:null as any};
  const isSuperAdmin=Boolean(me?.is_super_admin||user?.email?.toLowerCase()==="sarwar.khalid@miranenergy.com");
  const [mr,vr,pr,cr,projectResult]=await Promise.all([
    supabase.from("payment_milestones").select("id,purchase_order_id,milestone_name,percentage,fixed_amount,planned_due_date,payment_due_date,status,is_deleted").eq("is_deleted",false).order("planned_due_date"),
    supabase.from("vendors").select("id,vendor_name").eq("is_active",true).order("vendor_name"),
    supabase.from("purchase_orders").select("id,po_number,current_value,currency_id,vendor_id,project_id").eq("is_deleted",false),
    supabase.from("currencies").select("id,code"),
    supabase.from("projects").select("id,project_name,project_code").eq("is_deleted",false),
  ]);
  const allRows=mr.data??[]; const referencedPoIds=new Set(allRows.map((r:any)=>String(r.purchase_order_id||"")));
  const activePos=(pr.data??[]).filter((p:any)=>referencedPoIds.has(String(p.id))); const pos=new Map<string,any>(activePos.map((p:any)=>[String(p.id),p]));
  const currencies=new Map<string,string>((cr.data??[]).map((c:any)=>[String(c.id),String(c.code??"")]));
  const projects=new Map<string,string>((projectResult.data??[]).map((x:any)=>[String(x.id),String(x.project_name||x.project_code||"Unnamed Package")]));
  const vendors=(vr.data??[]).map((v:any)=>({id:String(v.id),vendor_name:String(v.vendor_name||"")})); const vendorMap=new Map(vendors.map(v=>[v.id,v.vendor_name]));
  const packageOptions=[...new Map(activePos.map((p:any)=>[String(p.project_id),{id:String(p.project_id),name:projects.get(String(p.project_id))||"Unnamed Package"}])).values()].sort((a,b)=>a.name.localeCompare(b.name));
  const selectedVendorId=q.vendor||"", selectedPackageId=q.package||"";
  let rows=allRows.filter((m:any)=>{const p=pos.get(String(m.purchase_order_id));return(!selectedVendorId||String(p?.vendor_id||"")===selectedVendorId)&&(!selectedPackageId||String(p?.project_id||"")===selectedPackageId)});
  const sort=q.sort||"due",dir=q.dir==="desc"?"desc":"asc",mul=dir==="desc"?-1:1;
  const value=(m:any,k:string)=>{const p=pos.get(String(m.purchase_order_id)); const amt=m.percentage!=null?Number(p?.current_value||0)*Number(m.percentage)/100:Number(m.fixed_amount||0); switch(k){case"vendor":return vendorMap.get(String(p?.vendor_id))||"";case"po":return p?.po_number||"";case"package":return projects.get(String(p?.project_id))||"";case"milestone":return m.milestone_name||"";case"percentage":return Number(m.percentage??-1);case"amount":return amt;case"due":return m.planned_due_date||"";case"payment_due":return m.payment_due_date||"";case"status":return m.status||"";default:return""}};
  rows=[...rows].sort((a:any,b:any)=>{const av=value(a,sort),bv=value(b,sort);return(typeof av==="number"&&typeof bv==="number"?(av-bv):String(av).localeCompare(String(bv)))*mul});
  const selectedVendor=vendors.find(v=>v.id===selectedVendorId), selectedPackage=packageOptions.find(p=>p.id===selectedPackageId);
  const summaryMap=new Map<string,{po_number:string;package_name:string;poValue:number;allocated:number;remaining:number;remainingAmount:number}>();
  for(const m of rows){const p=pos.get(String((m as any).purchase_order_id));if(!p)continue;const k=String(p.id);const cur=summaryMap.get(k)||{po_number:p.po_number,package_name:projects.get(String(p.project_id))||"—",poValue:Number(p.current_value||0),allocated:0,remaining:100,remainingAmount:Number(p.current_value||0)};const pct=(m as any).percentage!=null?Number((m as any).percentage):Number(p.current_value||0)?Number((m as any).fixed_amount||0)/Number(p.current_value||0)*100:0;cur.allocated+=pct;cur.remaining=Math.max(0,100-cur.allocated);cur.remainingAmount=Math.max(0,Number(p.current_value||0)*(cur.remaining/100));summaryMap.set(k,cur)}
  const sortHref=(key:string)=>{const p=new URLSearchParams();if(selectedVendorId)p.set("vendor",selectedVendorId);if(selectedPackageId)p.set("package",selectedPackageId);p.set("sort",key);p.set("dir",sort===key&&dir==="asc"?"desc":"asc");return`/payment-milestones?${p.toString()}`};
  const H=({k,children,cls=""}:{k:string;children:React.ReactNode;cls?:string})=><th className={`px-5 py-3 ${cls}`}><Link className="inline-flex items-center gap-1 hover:text-blue-700" href={sortHref(k)}>{children}<span>{sort===k?(dir==="asc"?"↑":"↓"):"↕"}</span></Link></th>;
  return <div className="mx-auto max-w-[1700px]">
    {q.deleted?<Notice kind="success">Milestone deleted. PO allocation has been recalculated from active milestones.</Notice>:null}{q.updated?<Notice kind="success">Milestone updated successfully.</Notice>:null}{q.bulk_updated?<Notice kind="success">All selected supplier milestones were updated successfully.</Notice>:null}{q.error?<Notice kind="error">{q.error}</Notice>:null}
    <div className="flex flex-wrap items-end justify-between gap-5"><div><div className="text-xs font-extrabold uppercase tracking-[0.2em] text-blue-700">MCCS</div><h1 className="mt-2 text-3xl font-bold">Payment Milestones</h1><p className="mt-2 text-sm text-slate-500">Contractual payment obligations, allocation and remaining PO exposure.</p></div><MilestoneVendorFilter vendors={vendors} packages={packageOptions} selectedVendorId={selectedVendorId} selectedPackageId={selectedPackageId}/></div>
    {(selectedVendor||selectedPackage)?<div className="mt-5 rounded-2xl border border-blue-100 bg-blue-50/70 px-5 py-4 text-sm text-blue-900">Showing {rows.length} active milestone{rows.length===1?"":"s"}{selectedPackage?<> for package <strong>{selectedPackage.name}</strong></>:null}{selectedVendor?<> from <strong>{selectedVendor.vendor_name}</strong></>:null}.</div>:null}
    <div className="mt-6 grid gap-4 md:grid-cols-3">{[...summaryMap.entries()].slice(0,9).map(([id,s])=>{const p=pos.get(id);return <div key={id} className="mccs-card rounded-2xl p-5"><div className="text-xs font-bold uppercase text-slate-500">{s.po_number}</div><div className="mt-1 text-sm font-semibold text-blue-700">{s.package_name}</div><div className="mt-3 rounded-xl bg-slate-50 px-3 py-2 text-sm"><span className="text-slate-500">PO Value:</span> <span className="font-bold text-slate-900">{currencies.get(p?.currency_id)||""} {s.poValue.toLocaleString()}</span></div><div className="mt-2 text-2xl font-bold">{s.allocated.toFixed(2)}% allocated</div><div className="mt-2 text-sm text-slate-500">Remaining: {s.remaining.toFixed(2)}% • {currencies.get(p?.currency_id)||""} {s.remainingAmount.toLocaleString()}</div></div>})}</div>
    {isSuperAdmin&&selectedVendor?<form action={bulkUpdateMilestones} className="mccs-card mt-6 overflow-hidden rounded-2xl border border-blue-100"><input type="hidden" name="vendor_id" value={selectedVendorId}/><div className="flex flex-wrap items-center justify-between gap-3 border-b bg-blue-50/70 px-5 py-4"><div><div className="text-xs font-extrabold uppercase tracking-[0.18em] text-blue-700">Super Admin Bulk Editor</div><h2 className="mt-1 text-xl font-bold">Edit milestones — {selectedVendor.vendor_name}{selectedPackage?` / ${selectedPackage.name}`:""}</h2></div><button className="rounded-xl bg-[#07111f] px-5 py-3 text-sm font-bold text-white">Save All Milestones</button></div><div className="overflow-x-auto"><table className="w-full min-w-[1450px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><H k="vendor">Supplier / Contractor</H><H k="po">PO</H><H k="package">Package</H><H k="milestone">Milestone</H><H k="percentage">%</H><H k="amount">Fixed Amount</H><H k="due">Milestone Due</H><H k="payment_due">Payment Due</H><H k="status">Status</H><H k="amount">Calculated</H></tr></thead><tbody className="divide-y">{rows.map((m:any)=>{const p=pos.get(String(m.purchase_order_id));const amt=m.percentage!=null?Number(p?.current_value||0)*Number(m.percentage)/100:Number(m.fixed_amount||0);return <tr key={m.id} className="align-top"><td className="px-4 py-3 font-semibold">{vendorMap.get(String(p?.vendor_id))||"—"}</td><td className="px-4 py-3 font-bold">{p?.po_number||"—"}<input type="hidden" name="milestone_id" value={m.id}/></td><td className="px-4 py-3 font-semibold">{projects.get(String(p?.project_id))||"—"}</td><td className="px-4 py-3"><input className="input min-w-[240px]" name={`milestone_name_${m.id}`} defaultValue={m.milestone_name||""} required/></td><td className="px-4 py-3"><input className="input" name={`percentage_${m.id}`} type="number" min="0" max="100" step="0.01" defaultValue={m.percentage??""}/></td><td className="px-4 py-3"><input className="input" name={`fixed_amount_${m.id}`} type="number" min="0" step="0.01" defaultValue={m.percentage==null?(m.fixed_amount??""):""}/></td><td className="px-4 py-3"><input className="input" name={`planned_due_date_${m.id}`} type="date" defaultValue={m.planned_due_date||""}/></td><td className="px-4 py-3"><input className="input" name={`payment_due_date_${m.id}`} type="date" defaultValue={m.payment_due_date||""}/></td><td className="px-4 py-3"><select className="input" name={`status_${m.id}`} defaultValue={m.status||"planned"}>{statuses.map(([v,l])=><option key={v} value={v}>{l}</option>)}</select></td><td className="px-4 py-4 font-semibold whitespace-nowrap">{currencies.get(p?.currency_id)||""} {amt.toLocaleString()}</td></tr>})}</tbody></table></div></form>:isSuperAdmin?<div className="mt-6 rounded-2xl border border-dashed border-blue-200 bg-white p-6 text-center"><div className="text-base font-bold">Bulk milestone editing is ready</div><div className="mt-1 text-sm text-slate-500">Select a Supplier / Contractor above to edit its payment milestones.</div></div>:null}
    <div className="mccs-card mt-6 overflow-hidden rounded-2xl"><div className="overflow-x-auto"><table className="w-full min-w-[1200px] text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><H k="vendor">Supplier / Contractor</H><H k="po">PO</H><H k="package">Package</H><H k="milestone">Milestone</H><H k="percentage">%</H><H k="amount">Calculated Amount</H><H k="due">Due</H><H k="payment_due">Payment Due</H><H k="status">Status</H><th className="px-5 py-3">Actions</th></tr></thead><tbody className="divide-y">{rows.map((m:any)=>{const p=pos.get(String(m.purchase_order_id));const amt=m.percentage!=null?Number(p?.current_value||0)*Number(m.percentage)/100:Number(m.fixed_amount||0);return <tr key={m.id}><td className="px-5 py-4 font-semibold">{vendorMap.get(String(p?.vendor_id))||"—"}</td><td className="px-5 py-4 font-bold">{p?.po_number||"—"}</td><td className="px-5 py-4 font-semibold">{projects.get(String(p?.project_id))||"—"}</td><td className="px-5 py-4">{m.milestone_name}</td><td className="px-5 py-4 font-semibold">{m.percentage!=null?`${Number(m.percentage).toFixed(2)}%`:"—"}</td><td className="px-5 py-4">{currencies.get(p?.currency_id)||""} {amt.toLocaleString()}</td><td className="px-5 py-4">{m.planned_due_date||"—"}</td><td className="px-5 py-4">{m.payment_due_date||"—"}</td><td className="px-5 py-4 capitalize">{m.status}</td><td className="px-5 py-4"><div className="flex gap-3"><Link href={`/payment-milestones/${m.id}/edit`} className="font-bold text-blue-700">Edit</Link>{isSuperAdmin?<SuperAdminDelete entity="milestone" entityId={m.id} entityLabel={m.milestone_name} idField="milestone_id" action={deletePaymentMilestone}/>:null}</div></td></tr>})}{!rows.length?<tr><td colSpan={10} className="px-5 py-12 text-center text-slate-500">No payment milestones match the selected filters.</td></tr>:null}</tbody></table></div></div>
  </div>
}
function Notice({kind,children}:{kind:"success"|"error";children:React.ReactNode}){const cls=kind==="success"?"border-emerald-200 bg-emerald-50 text-emerald-800":"border-red-200 bg-red-50 text-red-700";return <div className={`mb-5 rounded-xl border p-4 text-sm ${cls}`}>{children}</div>}
