# MCCS V2.1.2 Build Fix

Fixed the Vercel TypeScript error:

`app/api/admin/users/route.ts`
`'role' is possibly 'null'`

The user-admin API now:
- checks whether the requested role exists before accessing `role.id`
- checks Supabase errors for role/profile updates
- validates user IDs and passwords
- preserves Super Admin role handling
- keeps user creation, suspend, activate, password reset, delete and role-change functions
