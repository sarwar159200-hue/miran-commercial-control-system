import Link from "next/link";import {createClient} from "@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Page(){const s=await createClient();if(!s)return null;const r=await s.from("payments").select("id,purchase_order_id,currency_id,payment_date,paid_amount,payment_reference,bank_reference,is_historical").order("payment_date",{ascending:false});const rows=r.data??[],poIds=[...new Set(rows.map((x:any)=>x.purchase_order_id).filter(Boolean))],cIds=[...new Set(rows.map((x:any)=>x.currency_id).filter(Boolean))];const [pr,cr]=await Promise.all([poIds.length?s.from("purchase_orders").select("id,po_number").in("id",poIds):Promise.resolve({data:[]} as any),cIds.length?s.from("currencies").select("id,code").in("id",cIds):Promise.resolve({data:[]} as any)]);const pm=new Map<string,string>((pr.data??[]).map((x:any)=>[String(x.id),String(x.po_number)])),cm=new Map<string,string>((cr.data??[]).map((x:any)=>[String(x.id),String(x.code)]));return <div className="mx-auto max-w-[1600px]"><div className="flex items-end justify-between"><div><div className="text-xs font-extrabold uppercase tracking-[.2em] text-blue-700">Accounts Payable</div><h1 className="mt-2 text-3xl font-bold">Payments</h1><p className="mt-2 text-sm text-slate-500">Confirmed actual payments, historical entries and bank references.</p></div><Link href="/payments/new" className="rounded-xl bg-[#07111f] px-4 py-3 text-sm font-bold text-white dark:bg-blue-600">Record Payment</Link></div>{r.error?<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{r.error.message}</div>:null}<div className="mccs-card mt-7 overflow-hidden rounded-2xl"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Date</th><th className="px-5 py-3">PO</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Payment Ref</th><th className="px-5 py-3">Bank Ref</th><th className="px-5 py-3">History</th><th className="px-5 py-3"></th></tr></thead><tbody className="divide-y">{rows.map((x:any)=><tr key={x.id}><td className="px-5 py-4">{x.payment_date}</td><td className="px-5 py-4 font-bold">{pm.get(String(x.purchase_order_id))||"—"}</td><td className="px-5 py-4">{cm.get(String(x.currency_id))||""} {Number(x.paid_amount||0).toLocaleString()}</td><td className="px-5 py-4">{x.payment_reference||"—"}</td><td className="px-5 py-4">{x.bank_reference||"—"}</td><td className="px-5 py-4">{x.is_historical?<span className="rounded-full bg-amber-50 px-2 py-1 text-xs font-bold text-amber-700">Historical</span>:"—"}</td><td className="px-5 py-4"><Link href={`/payments/${x.id}/edit`} className="font-bold text-blue-700">Edit</Link></td></tr>)}{!rows.length?<tr><td colSpan={7} className="px-5 py-12 text-center text-slate-500">No payments recorded yet.</td></tr>:null}</tbody></table></div></div>}
