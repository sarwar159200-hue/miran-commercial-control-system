import Link from "next/link";import { createClient } from "@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Page(){const s=await createClient();if(!s)return null;const r=await s.from("payments").select("id,invoice_id,purchase_order_id,currency_id,payment_date,paid_amount,payment_reference,bank_reference").order("payment_date",{ascending:false});const rows=r.data??[];const poIds=[...new Set(rows.map((x:any)=>x.purchase_order_id))],cIds=[...new Set(rows.map((x:any)=>x.currency_id))];const [pr,cr]=await Promise.all([poIds.length?s.from("purchase_orders").select("id,po_number").in("id",poIds):Promise.resolve({data:[]} as any),cIds.length?s.from("currencies").select("id,code").in("id",cIds):Promise.resolve({data:[]} as any)]);const pm=new Map((pr.data??[]).map((x:any)=>[x.id,x.po_number])),cm=new Map((cr.data??[]).map((x:any)=>[x.id,x.code]));return <div className="mx-auto max-w-[1600px]"><div className="flex items-end justify-between"><div><div className="text-xs font-extrabold uppercase tracking-[0.2em] text-blue-700">Accounts Payable</div><h1 className="mt-2 text-3xl font-bold">Payments</h1><p className="mt-2 text-sm text-slate-500">Confirmed actual payments and references.</p></div><Link href="/payments/new" className="rounded-xl bg-[#07111f] px-4 py-3 text-sm font-bold text-white">Record Payment</Link></div><div className="mccs-card mt-7 overflow-hidden rounded-2xl"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Date</th><th className="px-5 py-3">PO</th><th className="px-5 py-3">Amount</th><th className="px-5 py-3">Payment Ref</th><th className="px-5 py-3">Bank Ref</th></tr></thead><tbody className="divide-y">{rows.map((x:any)=><tr key={x.id}><td className="px-5 py-4">{x.payment_date}</td><td className="px-5 py-4 font-bold">{pm.get(x.purchase_order_id)||"—"}</td><td className="px-5 py-4">{cm.get(x.currency_id)||""} {Number(x.paid_amount).toLocaleString()}</td><td className="px-5 py-4">{x.payment_reference||"—"}</td><td className="px-5 py-4">{x.bank_reference||"—"}</td></tr>)}{!rows.length?<tr><td colSpan={5} className="px-5 py-12 text-center text-slate-500">No payments recorded yet.</td></tr>:null}</tbody></table></div></div>}