import Link from "next/link"; import { createClient } from "@/lib/supabase/server"; import { POForm } from "./po-form";
export const dynamic="force-dynamic";
export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const params=await searchParams;const supabase=await createClient();if(!supabase)return null;const [vr,cr,pr]=await Promise.all([supabase.from("vendors").select("id,vendor_name,parent_vendor_id").eq("is_active",true).order("vendor_name"),supabase.from("currencies").select("id,code,name").eq("is_active",true).order("code"),supabase.from("projects").select("id,project_code,project_name").eq("is_active",true).order("project_name")]);return <div className="mx-auto max-w-6xl"><div className="flex items-end justify-between"><div><div className="text-xs font-extrabold uppercase tracking-[0.2em] text-blue-700">Purchase Order</div><h1 className="mt-2 text-3xl font-bold">Create New Purchase Order</h1></div><Link href="/purchase-orders" className="text-sm font-bold text-blue-700">Back to PO Register</Link></div><POForm vendors={vr.data??[]} currencies={cr.data??[]} projects={pr.data??[]} error={params.error}/></div>}