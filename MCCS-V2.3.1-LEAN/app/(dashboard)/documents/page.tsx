import Link from "next/link";import {createClient} from "@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Page(){const s=await createClient();if(!s)return null;const r=await s.from("documents").select("id,document_type,file_name,revision,onedrive_path,is_historical,uploaded_at,purchase_order_id,vendor_id").order("uploaded_at",{ascending:false});const rows=r.data??[];return <div className="mx-auto max-w-[1500px]"><div className="flex items-end justify-between"><div><div className="text-xs font-extrabold uppercase tracking-[.2em] text-blue-700">MCCS</div><h1 className="mt-2 text-3xl font-bold">Documents</h1><p className="mt-2 text-sm text-slate-500">Commercial document index. Metadata is live now; OneDrive file upload can be enabled when Microsoft Graph is configured.</p></div><Link href="/documents/new" className="rounded-xl bg-[#07111f] px-4 py-3 text-sm font-bold text-white dark:bg-blue-600">Add Document Record</Link></div>{r.error?<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-red-700">{r.error.message}</div>:null}<div className="mccs-card mt-7 overflow-hidden rounded-2xl"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Type</th><th className="px-5 py-3">File</th><th className="px-5 py-3">Revision</th><th className="px-5 py-3">Storage Path</th><th className="px-5 py-3">Historical</th><th className="px-5 py-3">Added</th></tr></thead><tbody className="divide-y">{rows.map((x:any)=><tr key={x.id}><td className="px-5 py-4 font-bold">{x.document_type}</td><td className="px-5 py-4">{x.file_name}</td><td className="px-5 py-4">{x.revision||"—"}</td><td className="px-5 py-4">{x.onedrive_path||"Not linked yet"}</td><td className="px-5 py-4">{x.is_historical?"Yes":"No"}</td><td className="px-5 py-4">{String(x.uploaded_at).slice(0,10)}</td></tr>)}{!rows.length?<tr><td colSpan={6} className="px-5 py-12 text-center text-slate-500">No commercial document records yet.</td></tr>:null}</tbody></table></div></div>}
