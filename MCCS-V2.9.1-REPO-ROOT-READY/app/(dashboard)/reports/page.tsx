import {createClient} from "@/lib/supabase/server";export const dynamic="force-dynamic";export default async function Page(){const s=await createClient();if(!s)return null;const [pr,ir,pyr,mr,cr]=await Promise.all([s.from("purchase_orders").select("id,current_value,currency_id,is_deleted"),s.from("invoices").select("invoice_amount,certified_amount,currency_id,status,due_date,is_deleted").eq("is_deleted",false),s.from("payments").select("paid_amount,currency_id,payment_date,is_deleted").eq("is_deleted",false),s.from("payment_milestones").select("status,payment_due_date,is_deleted").eq("is_deleted",false),s.from("currencies").select("id,code")]);const cm=new Map<string,string>((cr.data??[]).map((x:any)=>[String(x.id),String(x.code)]));function totals(rows:any[],field:string){const m=new Map<string,number>();for(const x of rows){const c=cm.get(String(x.currency_id))||"N/A";m.set(c,(m.get(c)||0)+Number(x[field]||0))}return [...m].sort()}const pos=(pr.data??[]).filter((x:any)=>!x.is_deleted), inv=ir.data??[], pay=pyr.data??[];const overdue=inv.filter((x:any)=>x.due_date&&new Date(x.due_date)<new Date()&&!['paid','submitted_to_ap'].includes(String(x.status))).length;return <div className="mx-auto max-w-[1500px]"><div className="flex items-end justify-between"><div><div className="text-xs font-extrabold uppercase tracking-[.2em] text-blue-700">Commercial Intelligence</div><h1 className="mt-2 text-3xl font-bold">Reports</h1><p className="mt-2 text-sm text-slate-500">Live multi-currency commercial position, invoice aging and milestone pipeline.</p></div><a href="/api/export/commercial" className="rounded-xl border bg-white px-4 py-3 text-sm font-bold dark:bg-slate-900">Export Excel</a></div><div className="mt-7 grid gap-4 md:grid-cols-4"><K l="Active POs" v={String(pos.length)}/><K l="Invoices" v={String(inv.length)}/><K l="Payments" v={String(pay.length)}/><K l="Overdue Invoices" v={String(overdue)}/></div><div className="mt-6 grid gap-5 xl:grid-cols-3"><Summary title="Committed by Currency" rows={totals(pos,'current_value')}/><Summary title="Certified by Currency" rows={totals(inv,'certified_amount')}/><Summary title="Paid by Currency" rows={totals(pay,'paid_amount')}/></div><div className="mccs-card mt-6 rounded-2xl p-6"><h2 className="text-lg font-bold">Milestone Pipeline</h2><div className="mt-4 flex flex-wrap gap-3">{[...new Map<string, number>((mr.data??[]).map((x:any)=>[String(x.status),0])).keys()].map(st=>{const n=(mr.data??[]).filter((x:any)=>String(x.status)===st).length;return <div key={st} className="rounded-xl border px-4 py-3"><div className="text-xs uppercase text-slate-500">{st.replaceAll('_',' ')}</div><div className="mt-1 text-2xl font-bold">{n}</div></div>})}</div></div></div>}
function K({l,v}:{l:string;v:string}){return <div className="mccs-card rounded-2xl p-5"><div className="text-xs font-bold uppercase text-slate-500">{l}</div><div className="mt-2 text-3xl font-bold">{v}</div></div>}function Summary({title,rows}:{title:string;rows:[string,number][]}){return <div className="mccs-card rounded-2xl p-6"><h2 className="font-bold">{title}</h2><div className="mt-4 space-y-3">{rows.map(([c,v])=><div key={c} className="flex justify-between border-b border-slate-100 pb-2"><span className="font-bold text-blue-700">{c}</span><span>{v.toLocaleString()}</span></div>)}{!rows.length?<div className="text-sm text-slate-500">No data</div>:null}</div></div>}
